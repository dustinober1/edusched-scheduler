# Backend production configuration

# Authentication
EDUSCHED_ALLOW_ANONYMOUS=false
EDUSCHED_JWT_SECRET=change-me
EDUSCHED_JWT_ISSUER=
EDUSCHED_JWT_AUDIENCE=

# Persistence
EDUSCHED_DB_PATH=data/edusched.sqlite3

# Optional: request size limit (bytes) - used by middleware default if you wire it into app.state
# EDUSCHED_MAX_BODY_BYTES=2000000
